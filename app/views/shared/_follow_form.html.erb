<% if user_signed_in? %>
  <div id="follow_form">
    <% if current_user.following_expedition?(@expedition) %>
      <%= render 'users/unfollow' %>
    <% else %>
      <%= render 'users/follow' %>
    <% end %>
  </div>
<% end %>